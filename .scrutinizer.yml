build:
  nodes:
    analysis:
      environment:
        php: 7.1
      project_setup:
        override: true
      tests:
        override: [php-scrutinizer-run]

checks:
  php:
    code_rating: true
    duplication: true
